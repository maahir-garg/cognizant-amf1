# Impact Lap Configuration
# In demo mode, the application runs 100% offline using deterministic cached responses in data/ai-cache/
DEMO_MODE=true

# Optional: Google Gemini API key for live generation. Leave empty to use cached offline fixtures.
GEMINI_API_KEY=

# Base URL for public API endpoints
NEXT_PUBLIC_APP_URL=http://localhost:3000
